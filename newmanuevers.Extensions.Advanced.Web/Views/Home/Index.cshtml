@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>C# Extension Methods</h1>
    <p class="lead">Extension Methods are a powerful C# feature which are underused and often
    misunderstood.  In this course I will leran all about writing, using and organising extension methods.
    </p>
    <p><a href="https://asp.net" class="btn btn-primary btn-lg">Learn More &raquo;</a></p>
</div>

<div class="row">
    <div class="col-md-4">
        <h2>Submit a form</h2>
        @using (Html.BeginForm("Submit", "Home", FormMethod.Post, new { id = "form1", IAutoFieldGenerator = true }))
        {
          <fieldset>
              <legend>Some UI Input</legend>
              <label for="field1">Field 1</label>
              <input type="text" id="field1" class="input-lg text-left" />
          </fieldset>

        <div>
            <p>
                <button id="btnRegister" type="submit" class="btn btn-default">Register</button>
                @Html.ValidationSummary(true);
            </p>
        </div>
        }
    </div>
</div>